//! Entry point for the `bubbles-tui` writer tool.
//!
//! Milestone 0 scaffold: the binary compiles and exits cleanly so the
//! workspace as a whole stays green while the TUI is built up in later
//! milestones.

const fn main() {
    // Real argv parsing and the terminal loop land in later milestones.
}
